package nimbus;

/** Launches the JavaFX application from a class that does not extend Application. */
public class Launcher {
    /** Starts Nimbus. */
    public static void main(String[] args) {
        Main.main(args);
    }
}
